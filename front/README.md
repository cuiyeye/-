# 三喜机器人产销存 · 前端原型

视觉与交互对齐 [强盛进销存](https://pmvitamin.com/projects/QS-JXC-Project/#/dashboard)：蓝顶栏、白卡片、状态 Tab、列表筛选、表单分区、**动作按钮改状态**。

业务字段与流程以 `01-context/00`～`08` 为准，不照搬进销存的仓库/审核/客户退货。

```bash
cd front
pnpm install
pnpm dev
```

浏览器打开终端提示的本地地址（默认 `http://localhost:5173`）。
